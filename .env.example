## Copy to `.env.local` (do not commit `.env.local`)
# Supabase (for cross-device sync)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Experimental chart drawing adapter (kept off by default)
# Set to true/1/on to enable local testing.
VITE_EXPERIMENTAL_LIGHTWEIGHT_DRAWING_ADAPTER=false
